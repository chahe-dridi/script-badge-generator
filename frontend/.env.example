# Copy to .env and adjust. Never commit the real .env.

# Base URL of the backend API (auth, plans, server ZIP fallback).
REACT_APP_API_URL=http://localhost:8000

# Dev server port (optional; CRA defaults to 3000). If you change this,
# update CORS_ORIGINS in the backend .env to match.
# PORT=3000
